
@{
    ViewData["Title"] = "Index";
}

<div class="jumbotron">
    <div class="container">
        <h1 class="display-4">Seja bem vindo ao Blog FK!</h1>
        <p>Aqui você encontrará diversos artigos relacionados a tecnologia. Confira nossos posts!</p>        
    </div>
</div>

<div class="container">
    <div class="alert alert-info show" id="connection-status">
    </div>

    <div class="blog-list row mb-2" id="blog-list">
    </div>

    <script id="blog-card" type="text/template">
        <div class="col-4">
            <div class="row no-gutters border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
                <div class="col p-4 d-flex flex-column position-static">
                    <strong class="d-inline-block mb-2 text-primary">{{PostId}}</strong>
                    <h3 class="mb-0">{{Title}}</h3>
                    <p class="card-text mb-auto">{{ShortDescription}}</p>
                    <a href="javascript:;" onclick="pageEvents.loadBlogPost('{{Id}}', '{{Link}}');" class="stretched-link">Ler</a>
                </div>
            </div>
        </div>
    </script>

    <div class="center" id="show-more">
        <button class="btn btn-primary" onclick="pageEvents.getMoreBlogPosts()">
            Ver Mais
        </button>
    </div>

    <div id="blog-item-container"></div>

    <script id="blog-item" type="text/template">
        <div id="{{Link}}" class="blog-item">
            {{Content}}
        </div>
        <a href="javascript:;" onclick="pageEvents.navigateBack()" class="btn btn-primary">Voltar</a>
    </script>

    <div class="modal fade" id="post-not-found" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Ops!</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Não encontramos este artigo no Cache. Você deseja que nós continuemos tentando fazer o download do artigo em segundo plano e te avisemos quando estiver disponível?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-success start-download" onclick="pageEvents.setBackgroundFetch('{{Id}}', '{{Link}}')">Sim</button>
                    <button type="button" class="btn btn-danger" data-dismiss="modal">Não</button>
                    <div id="download-status" style="display: none;">
                        <div class="progress">
                            <div class="progress-bar" style="width: 0%;"></div>
                        </div>
                        <div class="text-success">
                            <p>Pronto! Seu artigo está disponível para leitura</p>
                            <button class="btn btn-success" onclick="pageEvents.loadBlogPost('{{Id}}', '{{Link}}');">Ler</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>