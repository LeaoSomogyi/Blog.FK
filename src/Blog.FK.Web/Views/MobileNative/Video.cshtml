@model string
@{
    ViewData["Title"] = "Video";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-12 col-lg-12">
            <video src="@Url.Content($"~/static/{Model}")" controls autoplay>
                Seu navegador não suporta o elemento
                <code>video</code>.
            </video>
        </div>
    </div>
    <div class="row">
        <div class="col-2 col-lg-12">
            <a asp-action="Index" asp-controller="MobileNative"><i class="fa fa-arrow-left"></i> Voltar</a>
        </div>
    </div>
</div>